{
  "name": "@ultra-fast-builder/benchmarks",
  "version": "1.0.0",
  "description": "Performance benchmarks for UltraFastBuilder",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "benchmark": "node dist/index.js",
    "clinic": "clinic doctor -- node dist/index.js",
    "clinic:flame": "clinic flame -- node dist/index.js",
    "clinic:bubbleprof": "clinic bubbleprof -- node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@noony-serverless/type-builder": "workspace:*",
    "zod": "^4.0.0",
    "benchmark": "^2.1.4",
    "clinic": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/benchmark": "^2.1.0",
    "typescript": "^5.0.0"
  }
}
