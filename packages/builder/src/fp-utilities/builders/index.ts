/**
 * FP-Style Builders
 * Immutable builder pattern with functional approach
 */

export {
  createImmutableBuilder,
  mergeStates,
  cloneState,
  isEmpty,
  getKeys,
  hasKey,
  getValue,
  removeKey,
  updateKeys,
} from './immutable-builder';
