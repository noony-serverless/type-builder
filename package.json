{
  "name": "ultra-fast-builder",
  "version": "1.0.0",
  "description": "Ultra-fast TypeScript builder library with auto-detection for Zod, Classes, and Interfaces",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "benchmark": "turbo run benchmark",
    "clinic": "turbo run clinic",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0",
    "clinic": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
